name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Assemble site
        run: |
          mkdir _site
          cp index.html _site/
          cp gallery.html _site/ 2>/dev/null || true
          cp -r pages _site/pages
          rsync -a \
            --exclude 'compare-*' \
            --exclude '*.gslides-images' \
            --exclude 'gslides-*-images' \
            --exclude '*-uat-screenshots' \
            --exclude '*.compose' \
            --exclude 'prompt-images' \
            decks/ _site/decks/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
